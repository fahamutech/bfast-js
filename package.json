{
  "name": "bfastjs",
  "version": "4.0.0-beta.13",
  "description": "javascript client sdk for bfast::cloud project",
  "main": "dist/bfast.js",
  "maintainers": [
    "Fahamu Tech"
  ],
  "types": "dist/bfast.d.ts",
  "scripts": {
    "test": "mocha ./specs",
    "build": "npm run build:web && npm run build:node",
    "build:web": "npx gulp updateNode && npx webpack --config webpack.config.js",
    "publish:web": "npx gulp updateNode && npm run build:web && npm publish",
    "build:node": "npx gulp updateNode && cd node && webpack --config webpack-node.config.js",
    "publish:node": "npx gulp updateNode && npm run build:node && cd node && npm publish",
    "publish:all": "npm run publish:web && npm run publish:node"
  },
  "author": "Fahamu Tech",
  "license": "ISC",
  "dependencies": {
    "axios": "^0.19.2",
    "browser-or-node": "^1.3.0",
    "form-data": "^3.0.0",
    "localforage": "^1.9.0",
    "socket.io-client": "^2.3.0"
  },
  "devDependencies": {
    "@types/node": "^14.0.20",
    "@types/socket.io-client": "^1.4.33",
    "gulp": "^4.0.2",
    "mocha": "^8.1.1",
    "ts-loader": "^8.0.0",
    "typescript": "^3.9.6",
    "webpack": "^4.43.0",
    "webpack-cli": "^3.3.12"
  }
}
